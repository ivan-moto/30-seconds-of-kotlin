import io.kotlintest.properties.Gen
import io.kotlintest.properties.forAll
import io.kotlintest.specs.StringSpec

class ListTest: StringSpec() {

    init {

//        "bifurcate - total size of lists returned equals size of each input list" {
//            val listSize = 100
//            forAll(Gen.list(Gen.int()), Gen.list(Gen.bool()))
//            { list: List<Int>, filter: List<Boolean>  ->
//                bifurcate(list, filter).toList().flatten().size.let { resultSize ->
//                    (resultSize == list.size) && (resultSize == filter.size)
//                }
//            }
//        }
    }
}
